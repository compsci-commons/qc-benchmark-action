include: "rules/download.smk"
include: "rules/eval.smk"


rule download:
    input:
        reads,
        expand("test-regions.cov-{cov}.bed", cov=["low", "callable"]),
        "truth.vcf",


rule eval:
    input:
        happy_report